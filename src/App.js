import './App.css';
import useAxios from "./hooks/axios.hook";

function App() {
  // Example usage
  // const {response} = useAxios({url:'/network/1',method: 'get'})
  return (
    <div className="App">
      <p>MovieRoom App</p>
    </div>
  );
}

export default App;
